import React, { useEffect } from 'react'
import axios from "axios"

const btnDisplay = () => {


    /*
        return (<div className="chart" >
            <div className="btnBox">
                <div>
                    {select.bar
                        ? <button className="searchBtn select">BAR</button>
                        : <button onClick={handlerClick} className="searchBtn">BAR</button>}
                    {select.bar
                        ? <button onClick={world ? null : handlerClick} className="searchBtn">LINE</button>
                        : <button className="searchBtn select">LINE</button>}
                </div>
                <div className="countryDisplay">
                    <h4 className="country">Data for: World
                </h4>
                </div>
            </div>
            {select.world ? <Bar data={data} height={120} /> : null}
            {select.bar ? <Bar data={data} height={120} /> : null}
            {
                select.line
                    ? promiseInProgress ? <div className="loadingChart">
                        <CircularProgress color="secondary" /> </div>
                        : <Line data={line} height={120} />
                    : null
            }
        </div>)*/
}

export default btnDisplay
